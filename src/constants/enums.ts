export enum PieceType {
  EMPTY = 1,
  ENDIAN = 2,
  CURVE = 3,
  LINE = 4,
  FORK = 5,
  CROSS = 6
}

export enum Direction {
  TOP = 'top',
  RIGHT = 'right',
  BOTTOM = 'bottom',
  LEFT = 'left'
}
